# Environment variables (contain live credentials — never commit)
.env
.env.local
.env.*.local
.dev.vars.*

# Lockfiles: this project uses bun (bun.lock) — ignore npm/yarn/pnpm lockfiles
package-lock.json
yarn.lock
pnpm-lock.yaml

# Dependency directories
node_modules

# Build output
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
.vercel
*.local
*.tsbuildinfo

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Test coverage
coverage/
.nyc_output

# Temporary files / process artifacts
*.pid
*.log
tmp/
.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
Desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
